[package]
name = "autoclicker"
version = "0.1.0"
edition = "2021"

[dependencies]
fltk = { version = "1.4", features = ["fltk-bundled"] }

[profile.release]
opt-level = 3
lto = true
codegen-units = 1
strip = true
panic = "abort"