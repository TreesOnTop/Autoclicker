[package]
name = "autoclicker"
version = "0.1.0"
edition = "2024"

[dependencies]
fltk = { version = "1.4", features = ["fltk-bundled"] }
tray-icon = "0.24.1"

[profile.release]
opt-level = 3
lto = true
codegen-units = 1
panic = "abort"